{
  "name": "focusguard-saas",
  "version": "1.0.0",
  "private": true,
  "description": "Zero-Trust DNS filtering, DoH protection, parental controls and AdShield platform",
  "type": "module",
  "author": "Alberto Trujillo Mingorance <support@yourdomain.com>",
  "homepage": "https://focusguard.trujillomingorance.com",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/atrumin16/focusguard-saas.git"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && npx esbuild worker/src/index.ts --bundle --minify --legal-comments=none --outfile=dist/_worker.js --format=esm --target=es2022 --platform=neutral",
    "preview": "vite preview",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name=focusguard"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "terser": "^5.49.0",
    "typescript": "~6.0.2",
    "vite": "^8.2.0"
  }
}

